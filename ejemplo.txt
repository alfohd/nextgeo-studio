'use client';

import React, { useState, useEffect } from 'react';
import { ChevronDown, Code, Smartphone, Zap, Check, Menu, X, ExternalLink } from 'lucide-react';

export default function NextGeoStudio() {
  const [scrolled, setScrolled] = useState(false);
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [selectedPlan, setSelectedPlan] = useState<string | null>(null);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 50);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const scrollToSection = (id: string) => {
    document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' });
    setMobileMenuOpen(false);
  };

  const benefits = [
    { icon: Code, title: 'Diseño Profesional', desc: 'Sitios web modernos que destacan tu marca' },
    { icon: Smartphone, title: 'Responsive', desc: 'Perfectos en cualquier dispositivo' },
    { icon: Zap, title: 'Rápidos y Optimizados', desc: 'Carga veloz y SEO optimizado' }
  ];

  const faqs = [
    { q: '¿Cuánto tiempo toma crear mi sitio?', a: 'Entre 2-4 semanas dependiendo de la complejidad del proyecto.' },
    { q: '¿Incluye hosting y dominio?', a: 'Sí, todos nuestros planes incluyen hosting y dominio por el primer año.' },
    { q: '¿Puedo actualizar el contenido?', a: 'Absolutamente. Te enseñamos a gestionar tu sitio o lo hacemos por ti.' },
    { q: '¿Ofrecen soporte post-lanzamiento?', a: 'Sí, incluimos soporte técnico durante los primeros 3 meses.' }
  ];

  const plans = [
    { name: 'Básico', price: '250K', features: ['Diseño responsivo', 'Hasta 5 secciones', 'Formulario de contacto', 'SEO básico', 'Hosting 1 año'], popular: false },
    { name: 'Profesional', price: '450K', features: ['Todo lo básico', 'Hasta 10 secciones', 'Blog integrado', 'SEO avanzado', 'Analytics', 'Chat en vivo'], popular: true },
    { name: 'Premium', price: '750K', features: ['Todo lo profesional', 'Secciones ilimitadas', 'E-commerce básico', 'Diseño personalizado', 'Mantenimiento 3 meses', 'Soporte prioritario'], popular: false }
  ];

  const examples = [
    {
      title: 'Restaurante Gourmet',
      category: 'Gastronomía',
      description: 'Sitio elegante con menú digital, reservas online y galería de platos',
      image: 'https://images.unsplash.com/photo-1517248135467-4c7edcad34c4?w=800&h=600&fit=crop',
      features: ['Menú interactivo', 'Sistema de reservas', 'Galería'],
      color: 'from-orange-500 to-red-500'
    },
    {
      title: 'Boutique de Moda',
      category: 'E-commerce',
      description: 'Tienda online moderna con catálogo de productos y carrito de compras',
      image: 'https://images.unsplash.com/photo-1441986300917-64674bd600d8?w=800&h=600&fit=crop',
      features: ['Catálogo productos', 'Carrito de compras', 'Pagos online'],
      color: 'from-pink-500 to-purple-500'
    },
    {
      title: 'Estudio de Arquitectura',
      category: 'Portafolio',
      description: 'Portafolio minimalista con galería de proyectos y formulario de contacto',
      image: 'https://images.unsplash.com/photo-1503387762-592deb58ef4e?w=800&h=600&fit=crop',
      features: ['Portafolio visual', 'Diseño minimalista', 'Formulario contacto'],
      color: 'from-slate-500 to-gray-500'
    },
    {
      title: 'Gimnasio Fitness',
      category: 'Servicios',
      description: 'Sitio dinámico con horarios, clases y sistema de inscripción',
      image: 'https://images.unsplash.com/photo-1534438327276-14e5300c3a48?w=800&h=600&fit=crop',
      features: ['Horarios de clases', 'Inscripciones', 'Blog fitness'],
      color: 'from-green-500 to-emerald-500'
    },
    {
      title: 'Agencia de Viajes',
      category: 'Turismo',
      description: 'Plataforma interactiva con paquetes turísticos y sistema de cotización',
      image: 'https://images.unsplash.com/photo-1488646953014-85cb44e25828?w=800&h=600&fit=crop',
      features: ['Paquetes turísticos', 'Cotizaciones', 'Testimonios'],
      color: 'from-blue-500 to-cyan-500'
    },
    {
      title: 'Clínica Dental',
      category: 'Salud',
      description: 'Sitio profesional con información de servicios y agenda de citas',
      image: 'https://images.unsplash.com/photo-1588776814546-1ffcf47267a5?w=800&h=600&fit=crop',
      features: ['Servicios médicos', 'Agenda citas', 'Información equipo'],
      color: 'from-teal-500 to-blue-500'
    }
  ];

  return (
    <div className="bg-gradient-to-br from-slate-950 via-blue-950 to-slate-900 text-white min-h-screen">
      <style>{`
        @keyframes float {
          0%, 100% { transform: translateY(0px); }
          50% { transform: translateY(-20px); }
        }
        @keyframes fadeIn {
          from { opacity: 0; transform: translateY(20px); }
          to { opacity: 1; transform: translateY(0); }
        }
        .animate-float {
          animation: float 3s ease-in-out infinite;
        }
        .animate-fade-in {
          animation: fadeIn 1s ease-out;
        }
      `}</style>

      {/* Navigation */}
      <nav className={`fixed w-full z-50 transition-all duration-300 ${scrolled ? 'bg-slate-950/95 backdrop-blur-lg shadow-lg' : 'bg-transparent'}`}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-center h-20">
            <div className="flex items-center space-x-2">
              <div className="text-2xl font-bold bg-gradient-to-r from-blue-400 to-blue-600 bg-clip-text text-transparent">
                NextGeo
              </div>
            </div>
            
            {/* Desktop Menu */}
            <div className="hidden md:flex space-x-8">
              <button onClick={() => scrollToSection('inicio')} className="hover:text-blue-400 transition">Inicio</button>
              <button onClick={() => scrollToSection('beneficios')} className="hover:text-blue-400 transition">Beneficios</button>
              <button onClick={() => scrollToSection('ejemplos')} className="hover:text-blue-400 transition">Ejemplos</button>
              <button onClick={() => scrollToSection('preguntas')} className="hover:text-blue-400 transition">FAQ</button>
              <button onClick={() => scrollToSection('planes')} className="hover:text-blue-400 transition">Planes</button>
            </div>

            {/* Mobile Menu Button */}
            <button onClick={() => setMobileMenuOpen(!mobileMenuOpen)} className="md:hidden">
              {mobileMenuOpen ? <X size={24} /> : <Menu size={24} />}
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {mobileMenuOpen && (
          <div className="md:hidden bg-slate-950/98 backdrop-blur-lg">
            <div className="px-4 pt-2 pb-4 space-y-2">
              <button onClick={() => scrollToSection('inicio')} className="block w-full text-left py-2 hover:text-blue-400">Inicio</button>
              <button onClick={() => scrollToSection('beneficios')} className="block w-full text-left py-2 hover:text-blue-400">Beneficios</button>
              <button onClick={() => scrollToSection('ejemplos')} className="block w-full text-left py-2 hover:text-blue-400">Ejemplos</button>
              <button onClick={() => scrollToSection('preguntas')} className="block w-full text-left py-2 hover:text-blue-400">FAQ</button>
              <button onClick={() => scrollToSection('planes')} className="block w-full text-left py-2 hover:text-blue-400">Planes</button>
            </div>
          </div>
        )}
      </nav>

      {/* Hero Section */}
      <section id="inicio" className="min-h-screen flex flex-col items-center justify-center px-4 relative overflow-hidden">
        {/* Animated Background Elements */}
        <div className="absolute inset-0 overflow-hidden">
          <div className="absolute w-96 h-96 bg-blue-500/10 rounded-full blur-3xl -top-48 -left-48 animate-pulse"></div>
          <div className="absolute w-96 h-96 bg-purple-500/10 rounded-full blur-3xl -bottom-48 -right-48 animate-pulse"></div>
        </div>

        <div className="relative z-10 text-center space-y-8 animate-fade-in">
          {/* Logo */}
          <div className="flex flex-col items-center space-y-4">
            <div className="text-8xl sm:text-9xl font-bold bg-gradient-to-b from-blue-400 to-blue-600 bg-clip-text text-transparent animate-float">
              NextGeo
            </div>
            <div className="space-y-2">
              <h2 className="text-3xl sm:text-4xl font-light tracking-widest text-gray-400">STUDIO</h2>
            </div>
          </div>

          <p className="text-xl sm:text-2xl text-gray-300 max-w-2xl mx-auto px-4">
            Transformamos tus ideas en experiencias digitales extraordinarias
          </p>

          <button 
            onClick={() => scrollToSection('beneficios')}
            className="group mt-8 bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-500 hover:to-blue-600 px-8 py-4 rounded-full font-semibold text-lg transition-all duration-300 shadow-lg hover:shadow-blue-500/50 hover:scale-105"
          >
            Conocer más
            <ChevronDown className="inline ml-2 animate-bounce" size={20} />
          </button>
        </div>

        <div className="absolute bottom-8 left-1/2 transform -translate-x-1/2">
          <ChevronDown className="animate-bounce text-blue-400" size={32} />
        </div>
      </section>

      {/* Benefits Section */}
      <section id="beneficios" className="py-20 px-4 relative">
        <div className="max-w-6xl mx-auto">
          <h2 className="text-4xl sm:text-5xl font-bold text-center mb-4 bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
            ¿Por qué necesitas una página web?
          </h2>
          <p className="text-center text-gray-400 mb-16 max-w-2xl mx-auto">
            En el mundo digital actual, tu presencia online es tu carta de presentación. Una página web profesional es la clave para destacar.
          </p>

          <div className="grid md:grid-cols-3 gap-8">
            {benefits.map((benefit, idx) => (
              <div key={idx} className="group bg-slate-900/50 backdrop-blur-sm p-8 rounded-2xl border border-slate-800 hover:border-blue-500/50 transition-all duration-300 hover:transform hover:scale-105 hover:shadow-xl hover:shadow-blue-500/20">
                <benefit.icon className="text-blue-400 mb-4 group-hover:scale-110 transition-transform" size={48} />
                <h3 className="text-2xl font-bold mb-3">{benefit.title}</h3>
                <p className="text-gray-400">{benefit.desc}</p>
              </div>
            ))}
          </div>

          <div className="mt-16 bg-gradient-to-r from-blue-900/30 to-purple-900/30 backdrop-blur-sm p-8 rounded-2xl border border-blue-500/20">
            <h3 className="text-2xl font-bold mb-4">Tu presencia digital importa</h3>
            <p className="text-gray-300 leading-relaxed">
              Más del 80% de los consumidores investigan online antes de comprar. Una página web profesional genera confianza, 
              aumenta tu credibilidad y te abre las puertas a nuevos clientes las 24 horas del día. No es solo una inversión, 
              es una necesidad para cualquier negocio moderno.
            </p>
          </div>
        </div>
      </section>

      {/* Examples Section */}
      <section id="ejemplos" className="py-20 px-4 bg-slate-950/50">
        <div className="max-w-7xl mx-auto">
          <h2 className="text-4xl sm:text-5xl font-bold text-center mb-4 bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
            Nuestro Trabajo
          </h2>
          <p className="text-center text-gray-400 mb-16 max-w-2xl mx-auto">
            Explora algunos de los proyectos que hemos desarrollado para diferentes tipos de negocios
          </p>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {examples.map((example, idx) => (
              <div 
                key={idx} 
                className="group bg-slate-900/50 backdrop-blur-sm rounded-2xl border border-slate-800 hover:border-blue-500/50 transition-all duration-300 overflow-hidden hover:transform hover:scale-105"
              >
                {/* Image */}
                <div className="relative h-48 overflow-hidden">
                  <div className={`absolute inset-0 bg-gradient-to-br ${example.color} opacity-20 group-hover:opacity-30 transition-opacity`}></div>
                  <img 
                    src={example.image} 
                    alt={example.title}
                    className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                  />
                  <div className="absolute top-4 right-4 bg-black/70 backdrop-blur-sm px-3 py-1 rounded-full text-sm">
                    {example.category}
                  </div>
                </div>

                {/* Content */}
                <div className="p-6">
                  <h3 className="text-2xl font-bold mb-2 group-hover:text-blue-400 transition-colors">
                    {example.title}
                  </h3>
                  <p className="text-gray-400 mb-4">
                    {example.description}
                  </p>

                  {/* Features */}
                  <div className="flex flex-wrap gap-2 mb-4">
                    {example.features.map((feature, i) => (
                      <span 
                        key={i}
                        className="text-xs bg-slate-800 px-3 py-1 rounded-full text-blue-400"
                      >
                        {feature}
                      </span>
                    ))}
                  </div>

                  {/* CTA Button */}
                  <button className="flex items-center text-blue-400 hover:text-blue-300 transition-colors group/btn">
                    Ver demo
                    <ExternalLink className="ml-2 group-hover/btn:translate-x-1 transition-transform" size={16} />
                  </button>
                </div>
              </div>
            ))}
          </div>

          {/* CTA */}
          <div className="text-center mt-12">
            <p className="text-gray-400 mb-4">¿Te gusta lo que ves?</p>
            <button 
              onClick={() => scrollToSection('planes')}
              className="bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-500 hover:to-purple-500 px-8 py-3 rounded-full font-semibold transition-all duration-300 shadow-lg hover:shadow-blue-500/50 hover:scale-105"
            >
              Ver planes
            </button>
          </div>
        </div>
      </section>

      {/* FAQ Section */}
      <section id="preguntas" className="py-20 px-4 bg-slate-950/50">
        <div className="max-w-4xl mx-auto">
          <h2 className="text-4xl sm:text-5xl font-bold text-center mb-16 bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
            Preguntas Frecuentes
          </h2>
          
          <div className="space-y-4">
            {faqs.map((faq, idx) => (
              <details key={idx} className="group bg-slate-900/50 backdrop-blur-sm rounded-xl border border-slate-800 hover:border-blue-500/50 transition-all overflow-hidden">
                <summary className="cursor-pointer p-6 font-semibold text-lg list-none flex justify-between items-center">
                  {faq.q}
                  <ChevronDown className="group-open:rotate-180 transition-transform text-blue-400" size={24} />
                </summary>
                <div className="px-6 pb-6 text-gray-400 border-t border-slate-800 pt-4">
                  {faq.a}
                </div>
              </details>
            ))}
          </div>
        </div>
      </section>

      {/* Pricing Section */}
      <section id="planes" className="py-20 px-4">
        <div className="max-w-7xl mx-auto">
          <h2 className="text-4xl sm:text-5xl font-bold text-center mb-4 bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
            Planes a tu medida
          </h2>
          <p className="text-center text-gray-400 mb-16">Elige el plan perfecto para tu proyecto</p>

          <div className="grid md:grid-cols-3 gap-8">
            {plans.map((plan, idx) => (
              <div key={idx} className={`relative bg-slate-900/50 backdrop-blur-sm rounded-2xl border p-8 transition-all duration-300 hover:transform hover:scale-105 ${plan.popular ? 'border-blue-500 shadow-xl shadow-blue-500/20' : 'border-slate-800 hover:border-blue-500/50'}`}>
                {plan.popular && (
                  <div className="absolute -top-4 left-1/2 transform -translate-x-1/2 bg-gradient-to-r from-blue-600 to-purple-600 px-4 py-1 rounded-full text-sm font-semibold">
                    Más Popular
                  </div>
                )}
                
                <h3 className="text-2xl font-bold mb-2">{plan.name}</h3>
                <div className="mb-6">
                  <span className="text-5xl font-bold bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">${plan.price}</span>
                  <span className="text-gray-400"> COP</span>
                </div>

                <ul className="space-y-3 mb-8">
                  {plan.features.map((feature, i) => (
                    <li key={i} className="flex items-start">
                      <Check className="text-blue-400 mr-2 flex-shrink-0 mt-1" size={20} />
                      <span className="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>

                <button 
                  onClick={() => setSelectedPlan(plan.name)}
                  className={`w-full py-3 rounded-xl font-semibold transition-all duration-300 ${plan.popular ? 'bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-500 hover:to-purple-500 shadow-lg hover:shadow-blue-500/50' : 'bg-slate-800 hover:bg-slate-700'}`}
                >
                  Comenzar
                </button>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Contact/CTA Section */}
      <section className="py-20 px-4 bg-gradient-to-r from-blue-900/30 to-purple-900/30">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-4xl sm:text-5xl font-bold mb-6">
            ¿Listo para comenzar?
          </h2>
          <p className="text-xl text-gray-300 mb-8">
            Contáctanos hoy y lleva tu negocio al siguiente nivel
          </p>
          <a 
            href="https://wa.me/573012345678?text=Hola!%20Estoy%20interesado%20en%20crear%20una%20página%20web" 
            target="_blank"
            rel="noopener noreferrer"
            className="inline-block bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-500 hover:to-purple-500 px-12 py-4 rounded-full font-semibold text-lg transition-all duration-300 shadow-lg hover:shadow-blue-500/50 hover:scale-105"
          >
            Contactar ahora
          </a>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-slate-950 py-8 px-4 border-t border-slate-800">
        <div className="max-w-7xl mx-auto text-center text-gray-400">
          <p className="mb-2 font-bold text-blue-400">NextGeo Studio</p>
          <p>© 2024 Todos los derechos reservados</p>
        </div>
      </footer>

      {/* Selected Plan Modal */}
      {selectedPlan && (
        <div className="fixed inset-0 bg-black/80 backdrop-blur-sm flex items-center justify-center z-50 p-4" onClick={() => setSelectedPlan(null)}>
          <div className="bg-slate-900 rounded-2xl p-8 max-w-md w-full border border-blue-500/50" onClick={e => e.stopPropagation()}>
            <h3 className="text-2xl font-bold mb-4">Plan {selectedPlan}</h3>
            <p className="text-gray-400 mb-6">¡Excelente elección! Nos pondremos en contacto contigo pronto.</p>
            <button 
              onClick={() => setSelectedPlan(null)}
              className="w-full bg-gradient-to-r from-blue-600 to-purple-600 py-3 rounded-xl font-semibold"
            >
              Cerrar
            </button>
          </div>
        </div>
      )}
    </div>
  );
}